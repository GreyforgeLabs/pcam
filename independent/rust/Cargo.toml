[package]
name = "pcam-independent"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Independent Rust implementation lane for PCAM v3"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
unicode-normalization = "0.1"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
