# Experiment Results

`linux-x86_64.json` is the normalized raw result for the declared host. Its deterministic report digest is `e9133e751d263552d0545e45c4ca538d199b9ac6ee669a29b25c7899e175262d`.

All five subjects reached the same observable digest under all six raw contact permutations. The committed result records state and snapshot bytes, deterministic resimulation work, definition complexity, declared validation coverage, assumption and ambiguity counts, trace-field coverage, and an 11-replay host timing sample.

No performance ranking follows from these numbers. The adapters have deliberately different semantic breadth, and the timing evidence is descriptive for this exact environment only.
