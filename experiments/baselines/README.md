# Experiment Baselines

Baseline implementations are not yet present. Each baseline must remain behaviorally scoped to the shared experiment scenarios and must document any semantic mismatch that prevents a direct comparison.
