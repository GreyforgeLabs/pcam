# Reference Runtime

The executable Python runtime is packaged at `../python/pcam_runtime/runtime.py`. This directory is the stable repository lane required by PCAM v3 §41; it does not duplicate the implementation.
