# Vulcan Adversarial Review - 2026-08-27

Status: RESOLVED FOR REPORTED FINDINGS

Owner: PCAM integration

## Findings

1. High: `max_actions_per_entity` was declared but unenforced for slotless actions.
2. Medium: runtime snapshots encoded `entity_records`, `interaction_ledgers`, and `rng_streams` as keyed objects while the snapshot schema required arrays.

## Resolution evidence

- Every action start now claims the per-entity `ACTIONS` capacity. Atomic replacement releases and reacquires that capacity in the same intent.
- A regression starts three slotless actions under a limit of one and proves one acceptance plus two deterministic rejections.
- The snapshot schema now declares the three keyed collections as bounded objects, matching the canonical runtime encoding.
- A regression validates a runtime-produced snapshot through the repository snapshot schema.

The review occurred while generated tests were being added. Its observation that one generated test file was untracked became stale before closeout; that suite is committed separately.
