# Independent Implementation Gate

State: OPEN

Required: an implementation built from the specification and shared black-box vectors without sharing Python runtime execution code.

## Evidence present

- `independent/rust/` is a separately implemented Rust crate with no dependency on the Python runtime.
- `release/independent-code-audit.json` pins the exact Rust execution-source inventory, source, manifest, and lockfile hashes, direct dependency allowlist, and forbidden bridge or generation patterns. `reference/python/tests/test_independent_code_audit.py` fails on source drift, symlinks, build scripts, Python bindings or paths, subprocess execution, generated includes, dependency expansion, or Python bridge packages. The accompanying audit states the source-evidence boundary explicitly.
- `tests/vectors/pcam-cj1.json` supplies shared black-box canonical-byte and SHA-256 expectations.
- The Rust implementation passes all three PCAM-CJ1 cases, a real action-definition hash, and floating-point rejection.
- `tests/vectors/numeric-rng.json` supplies shared overflow, Euclidean division, checked ratio, PCG32 output, snapshot, and continuation expectations.
- Python and Rust independently agree on I64/U64 overflow policy, negative-floor division, checked intermediate overflow, five PCG32 draws, serialized stream state, restore, and the next draw.
- `tests/vectors/rng-runtime.json` integrates explicit transition `RNG_DRAW` effects into both complete-state runtimes. Three exact state digests pin canonical input-triggered draw ordering, Stage 10 execution, PCG32 values and mutable stream snapshots, while save and restore continuation pins the next draw and state digest.
- `tests/vectors/rng-call-order-runtime.json` drives two action instances against one explicitly named simulation stream. Reversing every raw start and draw input list preserves canonical action/effect order, assigns the same four PCG32 outputs, and produces identical stream snapshots and three state digests in Python and Rust. Midstream save/restore continues exactly. Both runtimes bind each stream algorithm to the hashed runtime profile and require the exact algorithm, state, selector, and draw-count fields; missing streams and U64 exhaustion are tick-atomic, while unknown or undeclared algorithms, missing or extra fields, and wrong field types fail before execution.
- `tests/vectors/expressions.json` exercises every core expression operator plus unresolved-reference, arity, float, division, overflow, depth, and node-budget failures in both languages.
- `tests/generated/core-properties-v1.json` is a reproducible seed-pinned §39 corpus executed by both languages. Its 336 cases cover random valid linear action graphs, transition guards, rational rates with repeat and save/restore continuation, raw same-tick input-order permutations, freeze-token activation, domain, target, HOLD/ACCRUE precedence and expiry combinations, retained rollback add/remove corrections with direct-execution equivalence, within-limit parent-child structures with repeat and restore checks, Euclidean division, checked ratio-floor semantics, I64/U64 overflow policies, SUM effect aggregation under permutations, candidate permutation canonicalization, and interaction-rule definition-order independence. It covers every bounded §39 generator category, adds generated Core numeric breadth, and directly checks repeat digest and continuation properties. `experiments/generate_core_properties.py --check` verifies the committed bytes.
- `tests/vectors/action-runtime.json` drives independent Python and Rust runtime paths through rational progression, PRE_ADVANCE, AFTER_QUANTUM, POST_ADVANCE, priority selection, explicit seek, terminal entry, predicate dependencies and edge serials, canonical input buffering and consumption, paired normal and `BUFFER_EXPIRY`-frozen TTL behavior, HOLD/ACCRUE and domain freezes, 14 invalid-definition cases with stable `DEFINITION_REJECTED`, deterministic runtime-limit and buffer-capacity faults, and canonical node/transition assignment and effect order with immutable matched-input context.
- `tests/vectors/state-distinction-runtime.json` supplies direct §38.1 and §38.2 complete-state evidence. Equal action phases with stall counters 4 and 1 or cycle values 0 and 12 produce distinct PCAM-CJ1 bytes, state hashes, and pinned continuation digests in Python and Rust. The Python vector loader preserves an explicitly empty effect registry as part of definition-set identity instead of silently substituting default effects.
- `tests/vectors/explicit-skip-runtime.json` supplies direct §38.6 complete-state evidence. Both runtimes land on seekable target step 3, commit exactly one declared transition `RESOURCE_DELTA`, emit no implicit continuation behavior, restore the final snapshot, and match three exact state digests.
- `tests/vectors/extension-tick-counter.json` drives the source-hash-verified authoritative extension pilot through the same counter outputs, restore continuation, tamper rejection, and checked overflow in Python and Rust.
- `tests/vectors/effects.json` drives all eight non-custom Core reducers, canonical group and source ordering, exclusive-effect rejection tracing, mixed-reducer rejection, unregistered-custom rejection, and checked SUM overflow in Python and Rust.
- `tests/vectors/custom-effect-ordered-fold.json` and `tests/vectors/custom-effect-runtime.json` close the bounded custom-authoritative effect surface in Python and Rust. The declarative ordered-I64-fold reducer binds the exact implementation source hash, canonical integer payload schema, canonical-effect ordering, checked I64 overflow, stateless save/restore, snapshot rollback, and a pinned determinism-vector hash into the effect-registry identity. Raw effect permutations reduce to the same order-sensitive value `257`; malformed payloads, overflow, source-hash or contract tampering, empty vector declarations, and missing registration fail closed. Complete-state execution folds entry payloads `2,7` to `27`, commits twice to `54`, pins the same definition-set hash and two state digests, restores after the first commit, and reproduces the final digest through corrected resimulation.
- `tests/vectors/events.json` drives canonical event ordering, due/future partitioning, action-scoped delivery-freeze deferral, unaffected entity delivery, continuation delivery, duplicate rejection, and late-event rejection in Python and Rust.
- `tests/vectors/freeze-stacking.json` drives all five Core freeze stack policies in Python and Rust. Eight cases pin canonical independent ordering, target and stack-group scope, matching-group replacement and rejection, MAX member retention, SUM activation queuing after the latest group expiration, and stable incompatible-group faults.
- `tests/vectors/event-delivery-runtime.json` integrates all five Core delivery modes into both complete-state runtimes. Shared route assertions cover action, parent, child, entity, and broadcast inboxes; a higher-priority false event-payload guard is rejected before true child and parent guards drive PRE and POST transitions; exact state digests pin canonical pending order, delivery, one-tick inbox cleanup, and snapshot restoration.
- `tests/vectors/transition-context-runtime.json` executes a complete-state expression predicate over owner resource, owner register, a bounded supplied action parameter, and a definition register initial, resolves a second predicate through `action.predicate` dependency context, and requires that derived predicate as a transition guard in Python and Rust. The transition expression additionally covers action identity, immutable input payload, and a declared typed host import. Accepted cases execute five sequential register-assignment groups from source exit through target entry, with the first assignment reading immutable matched-input context after buffer consumption. Source-exit, transition, and target-entry definition effects observe the intervening values and produce four exact envelopes: three presentation traces plus one authoritative SUM effect that alone reaches reduction and increments STAMINA. Provided true/false and `USE_DEFAULT` cases pin canonical state digests, predicate truth and entry serials, captured parameters, registers, emission serials, typed effects, reduction, resources, raw host snapshot persistence, transition serials, and retained-buffer TTL; a bounded U64 overflow case pins tick-start rollback and `FAULT_ACTION` containment. Missing required and wrong-typed imports map to stable contextual `INVALID_HOST_IMPORT` faults. Definition-fault cases reject predicate cycles, missing predicate dependencies, negative cycle or target-step values, non-seekable skips, out-of-range timed-node entry, duplicate transition priorities, missing source and guard references, illegal AFTER_QUANTUM claims, and missing cross-definition action targets before either complete-state runtime starts.
- `tests/vectors/initial-entry-effects-runtime.json` drives accepted direct, ACTION, and CHILD_ACTION starts through shared local operation order in Python and Rust. Four instances apply initial entry assignments before exact presentation and authoritative effects. The two transition-originated starts first execute source-exit and transition exit/body/entry assignments around source and transition effects, preserving the source emission serial while resetting the target serial. Child identity and linkage remain exact. Three pinned reductions commit deltas of 3, 3, and 4 at Stage 10, while six presentation envelopes remain trace-only; exact definition hashes, definition-set hash, action summaries, and three state digests close initial-action entry effects and ACTION/CHILD_ACTION local ordering.
- `tests/vectors/fault-target-runtime.json` drives the normative FAULT transition target through Python and Rust. The schema-required declared code is preserved by the full document adapter, included in canonical definition identity only for FAULT targets, validated as a canonical identifier, and stored as the action fault record instead of the transition ID. Exact local assignments and effects precede the lifecycle change, the authoritative delta still commits at Stage 10, and the trace contains no runtime-containment fault. Three state digests pin the accepted transition and a subsequent inert FAULTED tick. Missing, malformed, and non-FAULT codes fail definition construction.
- `tests/vectors/collection-register-runtime.json` drives all three Core collection register types through complete-state transition assignments in Python and Rust. SET_SYMBOL truncates under SATURATE and then sorts into canonical UTF-8 order, a later BOUNDED_LIST assignment observes that normalized set, FIXED_ARRAY enforces exact capacity, and another bounded list truncates deterministically. Exact presentation payload and state digests pin the successful result. Duplicate or non-symbol set members, wrong fixed length, FAULT-policy list overflow, and non-array values fault on the final sequential assignment; five contained digest pairs prove rollback of every earlier mutation and discarded output.
- `tests/vectors/event-effect-runtime.json` integrates transition-originated EVENT effects into Stage 10 in Python and Rust. The runtime derives `tick:source_action:effect` event IDs, writes current origin and next-tick delivery, validates type, delivery mode, and object payload, and stores the canonical envelope in authoritative pending state. Three exact digests prove no same-tick visibility, next-tick `TARGET_ACTION` delivery, event-payload guard execution, inbox cleanup, and inert empty pending state. A mid-pending save/restore produces the same delivery trace and final digest. Five invalid definitions fail closed, and duplicate same-tick creation rolls back before `FAULT_ACTION` containment with no pending envelope or emitted output.
- `tests/vectors/ledgers.json` drives all six Core hit policies through sequential eligibility and receipt writes in Python and Rust. It pins exact canonical keys, unbounded no-op receipts, cycle and predicate-entry expansion, cooldown reuse, contact partitions, all three receipt-condition outcomes, and fail-closed policy validation.
- `tests/vectors/ledger-runtime.json` integrates all six ledger policies into both complete-state runtimes. Exact multi-tick state proves unbounded repeated impact without receipts, once-per-action suppression, same-cycle duplicate rejection and reopening after checked `cycle_delta`, partition-specific contact receipts, cooldown reopening and receipt replacement, same-activation duplicate rejection and predicate-entry reopening after node exit and re-entry, and the expected `ON_CONTACT`, `ON_ACCEPT`, and `ON_IMPACT` timing.
- `tests/vectors/once-freeze-runtime.json` supplies direct §38.3 evidence in both complete-state runtimes. Four exact state digests prove one once-per-action impact, two active `INTERACTION_EMISSION` freeze ticks with no receipts or effects, token expiry, and post-freeze suppression by the original ledger receipt.
- `tests/vectors/arbitration.json` drives permutation-invariant canonical intent order, atomic multi-claim acceptance and rejection, resource reservation, action-slot replacement releases, child-slot ownership, capacity claims, exclusive keys, and accepted-order action identifier allocation in Python and Rust.
- `tests/vectors/interactions.json` drives the complete five-stage Core interaction resolver and all twelve Core rule operations in Python and Rust. It pins canonical candidate order, exact decision-record hashes, accepted transformation and reaction materialization, rejected-status reaction bounds, redirect restart with defense rebinding, redirect-limit rejection and faults, rule and operation stop controls, duplicate rule-order rejection, checked ratio faults, and unknown-operation rejection.
- `tests/vectors/interaction-runtime.json` integrates typed defense selection and the proven resolver into both complete-state tick runtimes. Exact state digests and trace summaries cover armor modification before materialization, parry rejection with bounded reaction materialization, decision tags, fired-rule order, impact receipts, ledger commit, effect reduction, and authoritative resource commit. The vector also guards typed interaction-template loading and default-expanded rule identity.
- `tests/vectors/simultaneous-trade-runtime.json` supplies direct §38.11, §38.12, and §38.13 complete-state evidence. Forward and reversed host contacts produce identical canonical order, effects, resources, and digests for a two-way lethal trade and for an armored fighter that halves incoming damage without deleting its outgoing lethal strike.
- `tests/vectors/redirect-runtime.json` integrates a two-hop `2 → 3 → 4` redirect into both complete-state runtimes. It proves frozen defense-map rebinding at each target, final-target armor modification, two resolver restarts, one impact receipt, final-target reduction and resource commit, unchanged intermediate banks, and an exact shared state digest.
- The unchanged `tests/vectors/typed-strike.json` now drives independent Python and Rust simulation runtimes through direct start, semantic predicate activation, canonical contact ordering, duplicate-contact ledger suppression, interaction effect materialization, SUM reduction, authoritative resource commit, complete-state PCAM-CJ1 digests for three ticks, snapshot round-trip, continuation, and corrected-input rollback equivalence. Rust independently derives the pinned definition and definition-set hashes.
- The Rust typed-strike runtime now also retains bounded per-tick snapshots and input/host documents, performs atomic correction replacement and resimulation, rejects corrections outside the retained window, and preserves prior history and head state when corrected replay faults.
- `tests/vectors/parent-child.json` drives both simulation runtimes through child-slot capacity, a `CHILD_ACTION` start, an active-child interval, parent freeze-token creation, child progression and termination, relationship cleanup, deterministic child-result scheduling, next-tick event delivery, event-matched parent transition, terminal entry, and four exact state digests. Both runtimes snapshot and restore while the child is still running, then prove identical remaining digests and final state. Rust independently derives both definition hashes and the definition-set hash.
- `tests/vectors/parent-policies.json` drives all five parent launch policies through three complete ticks in both runtimes. It pins direct-start versus pre-transition stage order, child linkage, `CONTINUE`, the domain-specific progression and transition freezes, the exact six-domain `FREEZE_ALL_ACTION_LOGIC` expansion, activation and end-tick duration updates, and `TERMINATE_PARENT` precedence while the newly launched child remains linked and running.
- `tests/vectors/child-termination-policies.json` drives all four child-slot termination policies through complete ticks in both runtimes. It pins recursive `TERMINATE_CHILD` plus child-result scheduling, `DETACH_CHILD`, retained linkage under `ALLOW_CHILD_TO_COMPLETE`, and `FAULT_IF_OCCUPIED` rejection before parent or child mutation.
- `tests/vectors/nesting-runtime.json` drives recursive `CHILD_ACTION` launches through complete ticks in both runtimes. Its bounded-success case pins a three-action parent chain, reciprocal child links, running lifecycle states, identifier allocation, and three exact state digests. Its limit case pins the pre-allocation state digest and contextual `NESTING_LIMIT_EXCEEDED` fault when a third level would exceed `max_action_nesting_depth`.
- `tests/vectors/contended-starts.json` integrates Core arbitration into both complete-state runtimes: both raw input permutations produce the same canonical winner and byte-equivalent final snapshot, canonical definition order overrides input sequence, a resource plus action-slot claim is reserved atomically, the losing start leaves no action or partial reservation, the slot record is rebuilt, one identifier is allocated, and the exact final digest matches. Rust independently derives both definition hashes and the definition-set hash.
- `tests/vectors/transition-replacement.json` integrates transition-originated arbitration into both complete-state runtimes. Its accepted case atomically releases the source capacity and action slot, reserves the target resource and slot claims, terminates the source, starts the target, rebuilds the slot, allocates one identifier, consumes the matched buffer entry, and matches both tick digests. Conflicting same-ID raw inputs pin the local profile's first-wins policy. Paired rejected cases preserve the source lifecycle, resource bank, slot, transition serial, and identifier counter exactly while proving that `ON_ATTEMPT` consumes the failed command and `ON_ACCEPT` retains it with the decremented authoritative TTL. Additional exact cases require both a true predicate guard and an immutable matched-input payload guard, then independently reject false predicate and false expression paths without consuming the buffered command.
- `tests/vectors/pre-stage-arbitration.json` proves that both complete-state runtimes merge pre-transition and direct-start intents before arbitration. Positive and negative transition-priority cases contend for the same resource and action slot, pin the canonical winner, allocate only the accepted target identifier, and match two exact state digests.
- `tests/vectors/multi-transition-arbitration.json` drives two simultaneously eligible transitions through one pre-stage arbitration pass. The higher-priority transition alone reserves the shared resource and action slot, the rejected source remains unchanged, accepted-order allocation produces the third action identifier, and both runtimes match the exact state.
- `tests/vectors/post-stage-arbitration.json` drives two simultaneously eligible `POST_ADVANCE` transitions through Stage 6 arbitration in both complete-state runtimes. The higher-priority transition alone reserves the shared resource and action slot and allocates its target, while exact per-tick state proves that the post-stage target is created at local step zero and first progresses on the following tick.
- `tests/vectors/fault-containment.json` drives Python and Rust policy-state implementations through `FAULT_ACTION`, `FAULT_ENTITY`, direct-start entity attribution, same-entity fanout, cross-entity parent/child detachment, entity fault records, tick advancement, and abort escalation for explicit or unattributable faults.
- `tests/vectors/fault-trigger-runtime.json` drives both complete-state runtimes through a real per-action quantum-limit fault after an earlier action has already progressed in the same tick. Exact pre-fault and contained-state digests prove tick-start rollback, discarded partial progression and output, `FAULT_ACTION` isolation, `FAULT_ENTITY` fanout, canonical fault traces, and `ABORT_SIMULATION` escalation without state mutation.
- `tests/vectors/interaction-fault-runtime.json` drives both complete-state runtimes through an attributed redirect-cycle fault after interaction processing begins. Exact pre-fault and contained-state digests prove stable `REDIRECT_LIMIT_EXCEEDED` mapping, candidate and source-action context, action isolation, same-owner entity fanout, discarded provisional ledger work, canonical fault traces, and abort immutability.
- `tests/vectors/effect-fault-runtime.json` drives both complete-state runtimes through an attributed Stage 10 SUM overflow after two accepted contacts have produced provisional receipts and authoritative effects. Exact pre-fault and contained-state digests prove stable `INTEGER_OVERFLOW` mapping, source-action attribution, action isolation, same-owner entity fanout, discarded receipts and effects, unchanged resources, canonical fault traces, and abort immutability.
- `tests/vectors/presentation-rollback.json` integrates authoritative and presentation effects into both complete-state runtimes, pins two state digests, excludes presentation from authoritative reduction and state, and drives retained replay through new emission, duplicate suppression, and invalidation using the same stable effect identifier.
- Every successful shared runtime case pins the same PCAM-CJ1 action-state projection digest in both languages, restores that state, advances one more tick, and pins the same continuation digest.
- Validation command: `cargo test --manifest-path independent/rust/Cargo.toml`.

## Remaining before closure

- Generalize the bounded independent simulation path to integrate the proven primitives across the complete tick stages, including broader interaction decisions, parent-child behavior, fault origins, snapshots, restore, and retained rollback.
- Produce matching Linux x86-64 and Linux ARM64 digest manifests.

The current evidence covers independent PCAM-CJ1, core numeric, PCG32, pure expressions, atomic Core arbitration, the complete standalone Core interaction rule language, all six standalone and complete-state Core ledger policies, every non-custom Core effect reducer, canonical events, all three fault-policy state transformations, the bounded authoritative extension pilot, a 336-case shared generated corpus across thirteen corpus families, a bounded local progression and transition-operation runtime, and exact complete-state typed-strike, explicit-skip, interaction, simultaneous-trade, redirected-interaction, ledger-policy, once-freeze, RNG, event-delivery, presentation, parent-child, parent-policy, child-termination, nesting-limit, contended-start, transition-replacement, pre-stage arbitration, post-stage arbitration, quantum-fault, interaction-fault, and effect-fault vectors. Those vectors integrate one-pass simultaneous pre-transition and direct-start arbitration, simultaneous post-transition arbitration with deferred target progression, replacement arbitration, checked cycle advance, explicit seek target steps and transition resource effects, explicit transition-effect RNG draws with snapshot continuation, all five Core event delivery modes with one-tick cleanup, frozen-snapshot lethal trades, armored incoming mitigation with preserved outgoing attacks, typed armor and parry decisions, two-hop redirection with defense rebinding and final-target commit, all six complete-state ledger-policy paths and all three receipt timings, direct once-per-action behavior across multiple interaction-emission freeze ticks, snapshots, retained correction and presentation reconciliation, all five parent launch policies, all four child termination policies, bounded recursive child launches, freeze activation and expiry, result-event delivery, and tick-start fault rollback for progression, interaction, and effect-reduction origins. Fault-origin integration and complete tick-pipeline breadth remain open, so the independent action-runtime gate remains open.
