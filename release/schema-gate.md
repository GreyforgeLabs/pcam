# Schema Gate

State: OPEN

Required: schemas for every normative definition type, positive and negative vectors, canonical-hash vectors, version checks, and extension checks.

Current evidence: six Draft 2020-12 schema documents parse successfully; initial positive and negative vectors pass targeted tests; a runtime-produced snapshot validates against the snapshot schema. Semantic completeness, canonical-hash corpus breadth, extension conformance, and all negative cases remain open.
