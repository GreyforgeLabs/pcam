# Rollback Gate

State: OPEN

Required: late input, misprediction, multi-tick rewind, action start, hit-stop, child action, RNG, ledger restoration, and presentation-event deduplication during rewind.
