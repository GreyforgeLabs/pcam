# PCAM v3 Requirements Matrix

Status: ACTIVE

Owner: PCAM release integration

This matrix preserves the complete target. A row is complete only when its named evidence directly proves the requirement.

| Spec area | Required evidence | Current state |
|---|---|---|
| §1-6 language, scope, invariants, determinism boundary, formal model | contradiction-free specification audit | complete: section, terminology, boundary, ordering, fault, PCAM-24 separation, and normative-issue audit is closed for the current candidate text |
| §7 authoritative state | snapshot schema plus complete-state round-trip vectors | partial: Python full-slice snapshots, cross-language canonical action-state projections, exact independent Rust complete-state round trips, and shared equal-phase state-distinction vectors pin different stall counters and cycle values to distinct serialization, hashes, and futures; broader independent simulation-state execution remains open |
| §8 definitions | action schema, immutable hash binding, positive and negative vectors | partial: the draft schema gate is closed with structurally typed definitions, semantic diagnostics, 23 deterministic mutation vectors, immutable Python hash binding, validated deep-captured parameters, typed host-import defaults/faults and snapshot capture, explicit empty effect-registry identity, and 14 shared Python/Rust invalid-action cases with stable `DEFINITION_REJECTED`; complete-state Python and Rust constructors additionally agree on predicate graph faults, transition bounds, duplicate transition priorities, missing source and guard references, illegal AFTER_QUANTUM claims, and missing cross-definition action targets, while broader cross-language full-definition binding remains open |
| §9 numeric semantics | arithmetic unit/property vectors including Euclidean division and overflow | complete for the bounded Core surface: Python and independent Rust agree on shared checked I64/U64 overflow, all three overflow policies, Euclidean division, exact ratio and negative-floor semantics, checked intermediates, and 88 reproducible generated division, ratio, and boundary-policy cases |
| §10 progression | rational accumulator and bounded quantum vectors | complete for the bounded Core surface: Python and independent Rust agree on rational accumulation, HOLD without accumulation, ACCRUE with authoritative deferred quanta, multi-quantum release after thaw, exact continuation digests, terminal stop behavior, and deterministic quantum and internal-transition limit faults |
| §11-12 expressions and predicates | pure evaluator, cycle validator, edge serial vectors | partial: Python and independent Rust agree on every bounded pure expression operator, predicate dependency execution, post-transition truth, sparse false-default truth state, edge serials, core faults, complete-state recursive expression-backed predicates and predicate guards, and true/false guard expressions over immutable selected input and event contexts; a shared complete-state context vector directly evaluates an expression predicate and a dependent predicate over owner resource, owner register, a bounded supplied action parameter, and a definition register initial, then additionally binds action identity, input payload, and a declared typed host import to accepted and rejected paths with exact digests, resolves a declared default, maps missing or wrong-typed imports to `INVALID_HOST_IMPORT`, and rejects predicate cycles or missing dependencies before simulation starts; broader hostile contextual graphs remain open |
| §13 inputs | canonical ordering, buffering, TTL, overflow, and consumption vectors | complete for the bounded Core surface: Python and independent Rust agree on assigned-tick filtering, canonical capture and selection, PRE/POST consumption, paired normal and `BUFFER_EXPIRY`-frozen TTL behavior, DROP_OLDEST, DROP_NEWEST, capacity faults, local-profile first-wins duplicate-ID deduplication, and complete-state rejected-attempt behavior that consumes under `ON_ATTEMPT` but retains under `ON_ACCEPT` |
| §14 transitions | all evaluation points, priorities, targets, mutation order, seek/skip vectors | partial: Python executes the complete declared assignment, cycle, effect, node-entry, and terminal-entry order with stable effect IDs, typed bounds, and fault atomicity; Python and independent Rust agree on that local NODE operation order plus all evaluation points, unique-priority selection, NODE/TERMINATE targets, explicit seek, terminal entry, continued quanta, internal-transition limits, checked complete-state `cycle_delta`, definition-time U64 checks for `cycle_delta` and `target_step`, non-seekable skip rejection, and timed-target bounds. Complete-state evidence executes sequential source-exit, transition-exit/body/entry, and target-entry assignments around source-exit, transition, and target-entry definition effects; payloads observe the exact preceding register and immutable matched-input state, emission IDs are stable, presentation effects remain non-authoritative, and an authoritative effect reduces and commits in Stage 10. A U64 overflow case proves tick-start rollback before `FAULT_ACTION` containment. Both runtimes also agree on accepted and rejected ACTION replacement and Stage 6 ACTION targets with next-tick progression; broader ACTION and CHILD_ACTION local operation ordering, initial-action entry effects, FAULT integration, and collection-register assignment types remain open |
| §15-16 intents, claims, slots | atomic arbitration and replacement vectors | partial: Python and independent Rust agree on permutation-invariant order, per-intent atomicity, resources, action slots, atomic replacement releases, child-slot ownership, capacity, exclusive keys, and accepted-order ID allocation; both integrate both raw permutations of contended direct starts with byte-equivalent final snapshots, accepted and atomically rejected transition-originated ACTION replacement, simultaneous transitions at PRE and POST stages, and positive and negative priority transition-versus-start contention through exact one-pass arbitration with matching action, bank, slot, transition serial, counter, and state digests, while a wider generated cross-stage corpus remains open |
| §17 parent-child | lifecycle, freeze, result event, nesting, restore vectors | partial: Python covers child slots, relationships, nesting bounds, every parent freeze, terminate-parent precedence, termination policy, next-tick result, and restore equivalence; Python and independent Rust agree exactly on bounded child-slot capacity, mid-child snapshot and continuation with live parent linkage, child slot, and parent progression freeze, same-tick and recursive child termination, freeze cleanup, result scheduling and delivery, terminal parent transition, all five parent launch policies, `TERMINATE_PARENT` precedence, all four child-slot termination policies including pre-mutation `FAULT_IF_OCCUPIED`, and recursive child launches through the configured nesting limit with a contextual pre-allocation fault beyond it |
| §18 freezes | domains, timing, expiry, stacking, accrual vectors | partial: Python and independent Rust agree on HOLD/ACCRUE progression plus PRE, POST, input-capture, and buffer-expiry domains; 24 shared generated token combinations cover activation, multiple targets and domains, HOLD precedence, exact expiry, and enumeration-order independence; eight shared stack vectors cover all five Core policies, target/group scope, canonical order, SUM queuing, and incompatible-group faults; complete-state vectors agree on next-tick activation, end-tick duration update, `FREEZE_PROGRESSION`, `FREEZE_TRANSITIONS`, the exact six-domain `FREEZE_ALL_ACTION_LOGIC` expansion, and direct `INTERACTION_EMISSION` suppression across two active ticks followed by expiry; broader runtime integration across every Core domain remains open |
| §19-22 facts, candidates, interactions, ledgers | frozen snapshot, typed stages, redirection, receipt vectors | partial: runtime-integrated semantic facts, explicit defense selection and ambiguity faults, five-stage resolver, accepted/rejected materialization, all ledger policies, receipts, and reducers; Python and independent Rust agree on canonical candidate order, all five stages, all twelve Core operations, exact decision-record hashes, transformation and reaction materialization, redirect restart and bounds, stop controls, deterministic faults, standalone sequential semantics and complete-state integration for all six Core ledger policies with exact keys and all three receipt conditions, exact complete-tick armor modification, parry rejection, frozen-snapshot lethal trade, armored mitigation with preserved outgoing attack, raw-contact permutation invariance, reaction impact, fired-rule trace, reduction and resource commit, and a two-hop redirected path with defense rebinding and final-target commit, while wider generated complete-pipeline interaction breadth remains open |
| §23 effects | identifiers, canonical order, reducers, exclusive/custom vectors | partial: runtime-integrated action, node, transition, and interaction emission with replay-stable identifiers, payload materialization, authoritative registry commits, all core reducer primitives, and rejection traces; independent Rust agrees on bounded local and interaction emission, stable identifiers, all non-custom Core reducers, canonical ordering, rejection traces, reducer faults, typed-strike authoritative resource commit, complete-state transition `RESOURCE_DELTA` commit, and attributed Stage 10 SUM-overflow rollback after provisional interaction work. Shared complete-state NODE evidence now expands source, transition, and target definition effects into exact envelopes with recursive payload references, owner-target resolution, monotonic emission serials, presentation exclusion, authoritative SUM reduction, and registry-backed resource commit; custom registration, initial-action entry-effect integration, and event effect creation remain open |
| §24 tick pipeline | deterministic twelve-stage trace vectors | partial: ordered Python execution plus a complete 22-field deterministic trace contract, cross-language local transition-operation order, one-pass pre-transition and direct-start arbitration, Stage 6 post-transition arbitration with deferred target progression, and exact independent typed-strike, parent-child, and contended-start complete-state simulation paths; independent full-pipeline breadth remains open |
| §25 events | delivery order, modes, and lifetime vectors | partial: Python and independent Rust agree on canonical due/future ordering, action-scoped one-tick freeze deferral, unaffected entity delivery, continuation, duplicate rejection, and late-event faults; both complete-state runtimes now route `TARGET_ACTION`, `TARGET_ENTITY`, `BROADCAST`, `PARENT`, and `CHILD`, reject a higher-priority false event-payload guard, execute true PRE and POST event-context transitions, clear one-tick action and entity inboxes, preserve canonical pending order, restore the snapshot, and match exact state digests, while broader effect-queued event creation remains open |
| §26 RNG | PCG32 vectors, snapshot state, restore vectors | partial: Python and independent Rust agree on canonical PCG32 outputs, snapshot state, profile validation, restore continuation, and draw-count overflow policy; both complete-state runtimes now execute explicit input-triggered transition `RNG_DRAW` effects in Stage 10 with exact draw traces, three state digests, mutable stream snapshots, restore, and next-draw continuation, while broader ownership scopes and generated call-order coverage remain open |
| §27 canonicalization and hashes | PCAM-CJ1 corpus and exact digest vectors | partial: Python and independent Rust serializers agree on shared exact-byte and SHA-256 vectors, a real definition, and every action-runtime state projection; wider hostile and cross-architecture corpora remain open |
| §28 save and restore | schema, round-trip, continuation equivalence | partial: Python full-slice restore, bounded retained rollback history, correction, and continuation plus cross-language action-state restore and exact independent Rust complete-simulation restore, continuation, bounded retained history, atomic corrected replay, retention-window rejection, and presentation reconciliation; broader independent runtime-state execution remains open |
| §29 networking | declared local, lockstep, rollback, and server prediction profile artifacts | partial: machine-readable topology declarations, required mechanism/limit fields, canonical profile hashing, and Python and Rust bounded rollback services with atomic replay, window enforcement, and stable-effect emit/suppress/invalidate reconciliation; lockstep and server-authoritative services remain open |
| §30-31 faults and limits | stable fault codes and every declared bound tested | partial: Python executes ABORT_SIMULATION, FAULT_ACTION, and FAULT_ENTITY with deterministic attribution, tick-start rollback, scoped lifecycle mutation, cross-entity detachment, trace records, snapshot continuation, discarded failed-tick output, and fail-closed escalation; Python and independent Rust now agree on action scope, entity fanout, direct-start attribution, cross-entity detachment, records, tick advance, and abort escalation, plus exact complete-state pre-fault and contained digests for progression quantum limits, interaction redirect cycles after provisional ledger work, and Stage 10 SUM overflow after provisional receipts and effects, while a broader independent fault-origin corpus remains open |
| §32 PCAM-24 | schema, compiler, lifecycle, projection, migration warnings | partial: schema, explicit TERMINATE/LOOP/CLAMP compiler, expression-backed projections, and warning-first v1/v2 migration; broader profile lifecycle vectors remain open |
| §33-34 examples | examples validate and execute with expected trace/digests | complete: schema-valid Heavy Strike, Dodge, and combat interaction documents execute for 60 ticks with pinned document, definition-set, trace, and final-state digests plus initial presentation emission, parry, armor, duplicate-contact, and trade assertions |
| §35 tracing | complete deterministic trace contract and canonicalized vectors | complete for the reference runtime: all 22 fields are machine-mapped, direct accepted/rejected/fault vectors pass, and the canonical 60-tick trace digest is pinned |
| §36 extensions | namespace, required/optional, authoritative extension validation | complete for the bounded reference surface: namespace and omission rules, declarative contracts, payload schemas, registry identity, source-hash verification, allowlisted hooks, tick-start runtime ordering, bounded snapshot state, restore, retained rollback, hostile depth and size rejection, and the shared counter corpus execute in Python and independent Rust |
| §37 classes | evidence manifest for each claimed class | complete: schema-valid manifest enumerates every class and exact requirement set, validates evidence paths and dependencies, and fail-closes premature claims; no class is currently claimed |
| §38 mandatory tests | all 20 tests implemented and passing | partial only on §38.19: §38.1-18 have direct exact shared cross-language vectors, direct rollback coverage spans every §45.6 case, and §38.20 combines 14 shared semantic invalid-action classes with stable `DEFINITION_REJECTED`, 23 deterministic structural/semantic schema mutations with pinned codes or faults, and dedicated invalid fixtures; Linux ARM64 execution remains unavailable |
| §39 generated tests | all listed generators plus determinism and continuation properties | complete for the bounded corpus: Python and independent Rust execute the same reproducible 336 cases spanning random valid linear action graphs, transition guards, rational rates and save/restore points, raw same-tick input-order permutations, freeze-token timing/domain/accrual combinations, retained rollback corrections, within-limit parent-child structures, generated Core arithmetic, effect aggregation permutations, candidate permutations, and bounded interaction-rule ordering; repeat digest and continuation equivalence are direct assertions |
| §40 migration | v1/v2 importer warnings and version rejection vectors | complete: bounded explicit-version importer, all required warning classes, deterministic evidence hashes, valid review-only PCAM-24 output, and fail-closed compatibility vectors |
| §41 repository | required structure and documentation | complete: every normative file and directory lane exists, packaged implementations have explicit stable adapters, archive lanes are non-authoritative, and a machine-checked layout manifest pins the inventory |
| §42 tooling | all eleven commands with stable machine-readable result codes | partial: all eleven command paths execute with JSON envelopes; broader normative vectors and diagnostics remain open |
| §43 licenses | CC BY 4.0 spec, Apache-2.0 or MIT code, vector permissions, trademark separation | complete: per-path license map, full CC BY 4.0, MIT, and CC0 1.0 texts, attribution, separate trademark policy, and explicit patent statement |
| §44 security | bounded untrusted-data validation and adversarial vectors | complete: machine-checked evidence covers every listed untrusted surface and bound, including pre-ingestion tick-input bytes, definition/snapshot bytes, runtime collections, nesting and parent cycles, candidates/effects/redirects, identifiers, integers, recursion, and extensions; hash authentication and extension sandboxing are explicitly disclaimed |
| §45 release gates | eight evidence reports with no unresolved requirement | partial: specification, schemas, rollback, bounded comparative experiment, and current-documentation claims gates are closed; independent-language execution evidence now includes a machine-checked source, dependency, bridge, subprocess, symlink, build-script, and generated-include audit; complete runtimes and ARM64 remain open |

## Completion rule

Search results, passing smoke tests, and plausible behavior are not enough. Each row requires the authoritative artifact and validation scope named above.
