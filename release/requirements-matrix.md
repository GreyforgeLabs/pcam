# PCAM v3 Requirements Matrix

Status: ACTIVE

Owner: PCAM release integration

This matrix preserves the complete target. A row is complete only when its named evidence directly proves the requirement.

| Spec area | Required evidence | Current state |
|---|---|---|
| §1-6 language, scope, invariants, determinism boundary, formal model | contradiction-free specification audit | complete: section, terminology, boundary, ordering, fault, PCAM-24 separation, and normative-issue audit is closed for the current candidate text |
| §7 authoritative state | snapshot schema plus complete-state round-trip vectors | complete for the bounded Core surface: Python and independent Rust serialize and restore the complete simulation state with exact canonical digests, while shared equal-phase vectors prove that distinct stall counters and cycle values remain distinct in bytes, hashes, and futures. The mixed-stage vector captures all global state families after child launch and interaction commit, including entity and action state, resources, relationships, buffers, events, freezes, ledgers, RNG and extension containers, fault state, host state, and both identifier generators. Both runtimes restore that mid-execution state exactly, preserve child and ledger identity, and produce the same continuation digests and effects |
| §8 definitions | action schema, immutable hash binding, positive and negative vectors | complete for the bounded Core surface: the draft schema gate is closed with structurally typed definitions, semantic diagnostics, 23 deterministic mutation vectors, validated deep-captured parameters, typed host-import defaults and faults with snapshot capture, explicit empty effect-registry identity, and 14 shared Python/Rust invalid-action cases with stable `DEFINITION_REJECTED`. Complete-state constructors additionally agree on predicate graph faults, transition bounds, duplicate priorities, missing source and guard references, illegal AFTER_QUANTUM claims, and missing cross-definition targets. A schema-valid full-definition document populates all 18 required top-level fields; one mutation per field changes the exact Python/Rust PCAM-CJ1 hash and produces one pinned aggregate digest. The schema adapter recursively freezes caller-owned metadata, declarations, expressions, effect payloads, fact attributes, transition metadata, and extensions, so later external mutation cannot change a constructed definition or its compiled runtime hash, while direct nested mutation fails closed |
| §9 numeric semantics | arithmetic unit/property vectors including Euclidean division and overflow | complete for the bounded Core surface: Python and independent Rust agree on shared checked I64/U64 overflow, all three overflow policies, Euclidean division, exact ratio and negative-floor semantics, checked intermediates, and 88 reproducible generated division, ratio, and boundary-policy cases |
| §10 progression | rational accumulator and bounded quantum vectors | complete for the bounded Core surface: Python and independent Rust agree on rational accumulation, HOLD without accumulation, ACCRUE with authoritative deferred quanta, multi-quantum release after thaw, exact continuation digests, terminal stop behavior, and deterministic quantum and internal-transition limit faults |
| §11-12 expressions and predicates | pure evaluator, cycle validator, edge serial vectors | complete for the bounded Core surface: Python and independent Rust agree on every pure operator and Core reference family used by the runtime, immutable selected input/event context, owner resources and registers, action parameters and registers, typed host imports, dependent predicate evaluation, sparse false defaults, and exact edge serials through transition, restore, and continuation. The shared hostile graph corpus adds forward and reversed diamond enumeration, chains exactly at and beyond the profile depth limit, predicate counts exactly at and beyond the node limit, self and three-node cycles, missing dependencies, tick-atomic rejection, and repeated false-to-true reactivation. Predicate graph depth and count now consume the hashed expression budgets instead of resetting limits at every dependency |
| §13 inputs | canonical ordering, buffering, TTL, overflow, and consumption vectors | complete for the bounded Core surface: Python and independent Rust agree on assigned-tick filtering, canonical capture and selection, PRE/POST consumption, paired normal and `BUFFER_EXPIRY`-frozen TTL behavior, DROP_OLDEST, DROP_NEWEST, capacity faults, local-profile first-wins duplicate-ID deduplication, and complete-state rejected-attempt behavior that consumes under `ON_ATTEMPT` but retains under `ON_ACCEPT` |
| §14 transitions | all evaluation points, priorities, targets, mutation order, seek/skip vectors | complete for the bounded Core surface: Python and independent Rust agree on complete local operation order for NODE, ACTION, CHILD_ACTION, TERMINATE, and FAULT targets; all evaluation points; unique-priority selection; explicit seek and timed bounds; continued quanta and transition limits; checked `cycle_delta`; and definition-time U64 validation. Exact complete-state evidence executes sequential source-exit, transition-exit/body/entry, target-entry, and accepted-start initial-entry assignments around definition effects with immutable matched context, stable serials, presentation exclusion, and Stage 10 authoritative commit. ACTION and CHILD_ACTION evidence pins source serial continuation, target serial reset, target initial operations, replacement arbitration, and post-stage next-tick progression. Declared FAULT evidence preserves its schema-required hash-bound code, executes preceding operations and effects, records the code without a runtime-containment trace, and leaves the FAULTED action inert. SET_SYMBOL, FIXED_ARRAY, and BOUNDED_LIST assignments now cover canonical set order, sequential observation, exact and bounded capacities, SATURATE truncation, invalid member/type/length rejection, and tick-start rollback before `FAULT_ACTION` containment; numeric overflow has the same atomic evidence |
| §15-16 intents, claims, slots | atomic arbitration and replacement vectors | complete for the bounded Core surface: Python and independent Rust agree on permutation-invariant order, per-intent atomicity, resources, action slots, atomic replacement releases, child-slot ownership, capacity, exclusive keys, and accepted-order ID allocation. Both integrate both raw permutations of contended direct starts with byte-equivalent final snapshots, accepted and atomically rejected transition-originated ACTION replacement, simultaneous transitions at PRE and POST stages, and positive and negative priority transition-versus-start contention through exact one-pass arbitration with matching action, bank, slot, transition serial, counter, and state digests. Twenty-four seed-pinned cross-stage cases vary priority, resource amount, and raw input order while proving deterministic winners, atomic resource and slot claims, stable instance allocation, exact restore, and permutation-invariant snapshots and digests |
| §17 parent-child | lifecycle, freeze, result event, nesting, restore vectors | complete for the bounded Core surface: Python and independent Rust agree on child and parent identity, reciprocal links, slot ownership and capacity, mid-child snapshot continuation, all five parent launch policies, explicit freeze tokens and exact `FREEZE_ALL_ACTION_LOGIC` expansion, next-tick child-result scheduling and delivery, same-tick and recursive termination, freeze cleanup, `TERMINATE_PARENT` launch precedence, all four parent termination policies including pre-mutation `FAULT_IF_OCCUPIED`, and recursive launch through the hashed nesting limit with a contextual pre-allocation fault beyond it. Definition slot capacities cannot exceed `max_children_per_action`. A shared two-slot vector distinguishes per-slot usage from profile-wide active-child count: both children launch when the limit is two, while limit one faults atomically before any externally visible second-child mutation with the same pre-fault digest in both runtimes |
| §18 freezes | domains, timing, expiry, stacking, accrual vectors | complete for the bounded Core surface: Python and independent Rust agree on all ten declared domains. A shared all-domain token vector proves identical next-tick activation, target scoping, exact two-tick duration, end-tick decrement, and expiry, including for `RESOURCE_REGENERATION`. Twenty-four generated token combinations cover multiple targets and domains, HOLD precedence, expiry, and enumeration-order independence; eight shared stack vectors cover all five Core policies, target/group scope, canonical order, SUM queuing, and incompatible-group faults. Complete-state vectors integrate every domain with a Core producer or consumer, including targeted interaction and Stage 10 RNG suppression while unaffected actions continue; save/restore preserves expiry and continuation. Core defines no resource-regeneration producer operation, so that domain's conformance is intentionally limited to token semantics rather than invented effect behavior |
| §19-22 facts, candidates, interactions, ledgers | frozen snapshot, typed stages, redirection, receipt vectors | complete for the bounded Core surface: runtime-integrated semantic facts, explicit defense selection and ambiguity faults, five-stage resolution, accepted and rejected materialization, all ledger policies, receipts, and reducers. Python and independent Rust agree on canonical candidate order, all five stages, all twelve Core operations, exact decision-record hashes, transformation and reaction materialization, redirect restart and bounds, stop controls, deterministic faults, standalone sequential semantics and complete-state integration for all six Core ledger policies with exact keys and all three receipt conditions, exact complete-tick armor modification, parry rejection, frozen-snapshot lethal trade, armored mitigation with preserved outgoing attack, raw-contact permutation invariance, reaction impact, fired-rule trace, reduction and resource commit, and a two-hop redirected path with defense rebinding and final-target commit. Twenty-four seed-pinned complete-pipeline cases evenly cover plain impact, armor modification, and parry rejection while varying payloads, initial resources, and raw duplicate-contact order; both runtimes agree on canonical selection, once-per-action receipts, typed materialization, reduction, commit, exact restore, and final-state digest invariance |
| §23 effects | identifiers, canonical order, reducers, exclusive/custom vectors | complete for the bounded Core surface: runtime-integrated action, node, transition, interaction, RNG, and event emission has replay-stable identifiers, payload materialization, authoritative commits, all Core reducers, and rejection traces. Python and independent Rust agree on local and interaction emission, typed-strike and transition commits, Stage 10 next-tick EVENT queueing, and attributed rollback after provisional work. Shared NODE evidence expands source, transition, target, and accepted-start initial-entry effects into exact envelopes with recursive payload references, owner-target resolution, per-instance emission serials, presentation exclusion, authoritative reduction, and registry-backed resource commit. The bounded `CUSTOM_DETERMINISTIC` pilot declares and hash-binds its canonical I64 payload schema, fixed executable reducer, canonical ordering, checked overflow, stateless save/restore, snapshot rollback, and determinism vector. Both runtimes produce the same order-sensitive reduction, definition-set hash, resource commits, state digests, restore continuation, and corrected resimulation, while tampered, malformed, omitted, and unregistered contracts fail closed |
| §24 tick pipeline | deterministic twelve-stage trace vectors | complete for the bounded Core surface: Python executes and names all twelve stages in exact order under the complete 22-field trace contract, while Python and independent Rust agree on local transition order, one-pass Stage 4 arbitration, Stage 6 deferred progression, parent-child maintenance, interaction resolution, effect commit, and final digests. The shared mixed-stage vector starts parent and defense actions, accepts a PRE_ADVANCE child launch, progresses the new child, exposes its same-tick semantic fact, resolves and commits an interaction receipt and damage effect, terminates the child, schedules and delivers its result event, and terminates the parent across four exact state digests. Reversing raw start enumeration preserves every final state and digest |
| §25 events | delivery order, modes, and lifetime vectors | complete for the bounded Core surface: Python and independent Rust agree on canonical due/future ordering, action-scoped one-tick freeze deferral, unaffected entity delivery, continuation, duplicate rejection, and late-event faults. Both complete-state runtimes route `TARGET_ACTION`, `TARGET_ENTITY`, `BROADCAST`, `PARENT`, and `CHILD`; reject a higher-priority false event-payload guard; execute true PRE and POST event-context transitions; clear one-tick action and entity inboxes; preserve canonical pending order; and restore pending events exactly. Stage 10 EVENT effects now derive replay-stable IDs, force current origin and next-tick delivery, queue canonical authoritative envelopes, remain invisible in their creation tick, drive a guarded transition on delivery, survive save/restore, and reject same-tick duplicate creation atomically |
| §26 RNG | PCG32 vectors, snapshot state, restore vectors | complete for the bounded Core surface: Python and independent Rust agree on canonical PCG32 outputs, exact four-field stream snapshots, runtime-profile algorithm binding, strict initial-state and restore validation, mutable stream state, checked draw-count overflow, and next-draw continuation. Both complete-state runtimes execute only explicit transition `RNG_DRAW` effects at Stage 10. A shared-stream two-action vector reverses raw start and command enumeration while preserving action allocation, canonical effect order, four exact assigned outputs, draw counts, three state digests, and save/restore continuation. Missing streams and exhausted draw counts fault without mutating tick-start state; malformed fields, unknown algorithms, undeclared profiles, wrong types, and extra fields fail before execution. The bounded Core exposes no candidate-local RNG operation, so candidate and hash-map enumeration cannot add an implicit draw path; broader ownership labels remain recommended rather than normative |
| §27 canonicalization and hashes | PCAM-CJ1 corpus and exact digest vectors | complete for the bounded Core surface: Python and independent Rust serializers agree on the original exact-byte and SHA-256 vectors, a real definition, every action-runtime state projection, all 18 full-definition fields, and a 31-case hostile corpus mapped across every PCAM-CJ1 rule. The hostile corpus pins Unicode NFC and UTF-8 key order, all control escapes, integer boundaries, array order, absent-versus-null identity, extension ordering, native set order, non-string logical-map order, whitespace removal, and direct normalized Unicode. Raw JSON entry points deterministically reject negative zero before sign loss, floats and non-finite constants, values outside the PCAM I64/U64 domain, duplicate and NFC-colliding keys, leading zero, trailing data, lone surrogates, invalid UTF-8, and BOM input; normalized set and logical-map collisions also fail closed. Cross-architecture state-digest execution remains tracked separately under §38.19 |
| §28 save and restore | schema, round-trip, continuation equivalence | complete for the bounded Core surface: Python and independent Rust round-trip complete simulation snapshots, preserve pending state and every identity-bearing relationship, and reproduce continuation state digests and effects. The mixed-stage vector snapshots after child launch, freeze activation, semantic interaction, ledger receipt, and resource commit, then restores and continues through child termination, result-event delivery, parent transition, and maintenance with exact equality. Its retained-history correction removes the launch input, reuses the immutable host contact snapshot, discards the now-inapplicable interaction, and matches direct execution byte-for-byte with pinned replay digests; bounded-window rejection and presentation reconciliation remain separately proven |
| §29 networking | declared local, lockstep, rollback, and server prediction profile artifacts | complete for the bounded Core surface: machine-readable declarations bind all four topologies, required mechanisms and finite limits, canonical profile hashing, snapshot rollback, and stable-effect reconciliation. Shared Python/Rust service vectors prove lockstep WAIT without tick advance, declared no-input prediction, canonical peer-input merge, definition-set identity, deterministic host-snapshot agreement, digest cadence, matching exchange, abort-on-desync, and atomic mismatch behavior. Server-authoritative vectors prove correction-window enforcement, authoritative tick assignment, restore-and-resimulate equivalence, complete-state replace-and-discard behavior, and rejection of future, stale, or incomplete corrections. Phase-only reconciliation remains prohibited; cross-architecture digest execution is tracked separately under §38.19 |
| §30-31 faults and limits | stable fault codes and every declared bound tested | complete for the bounded Core surface: Python executes ABORT_SIMULATION, FAULT_ACTION, and FAULT_ENTITY with deterministic attribution, tick-start rollback, scoped lifecycle mutation, cross-entity detachment, trace records, snapshot continuation, discarded failed-tick output, and fail-closed escalation. Python and independent Rust agree on action scope, entity fanout, direct-start attribution, cross-entity detachment, records, tick advance, and abort escalation, plus exact complete-state pre-fault and contained digests for progression quantum limits, interaction redirect cycles after provisional ledger work, and Stage 10 SUM overflow after provisional receipts and effects. Twenty-seven seed-pinned cases cover every combination of those three integrated origins and three policies with varied quantum and redirect limits, overflow magnitudes, initial resources, and raw enumeration order; both runtimes preserve the pre-fault state on abort, discard provisional ledgers, effects, and resource changes on containment, apply exact action or entity scope, restore exactly, and produce permutation-invariant states and digests |
| §32 PCAM-24 | schema, compiler, lifecycle, projection, migration warnings | complete for the bounded profile: the schema enforces the 24-cell half-open non-wrapping domain and permits overlapping tags; the compiler emits a seekable Core timeline, expression predicates, projection metadata, and explicit TERMINATE, LOOP, and CLAMP transitions. A shared Python/Rust lifecycle vector executes all 24 cells, exact termination, loop cycle increment and predicate reactivation serials, and CLAMP at timeline step 23 with authoritative rate zero and inert continuation. Explicit skip evidence fires no crossed semantics, migration is warning-first, and the compatibility statement rejects phase-only complete-state claims |
| §33-34 examples | examples validate and execute with expected trace/digests | complete: schema-valid Heavy Strike, Dodge, and combat interaction documents execute for 60 ticks with pinned document, definition-set, trace, and final-state digests plus initial presentation emission, parry, armor, duplicate-contact, and trade assertions |
| §35 tracing | complete deterministic trace contract and canonicalized vectors | complete for the reference runtime: all 22 fields are machine-mapped, direct accepted/rejected/fault vectors pass, and the canonical 60-tick trace digest is pinned |
| §36 extensions | namespace, required/optional, authoritative extension validation | complete for the bounded reference surface: namespace and omission rules, declarative contracts, payload schemas, registry identity, source-hash verification, allowlisted hooks, tick-start runtime ordering, bounded snapshot state, restore, retained rollback, hostile depth and size rejection, and the shared counter corpus execute in Python and independent Rust |
| §37 classes | evidence manifest for each claimed class | complete: schema-valid manifest enumerates every class and exact requirement set, validates evidence paths and dependencies, and fail-closes premature claims; no class is currently claimed |
| §38 mandatory tests | all 20 tests implemented and passing | partial only on §38.19: §38.1-18 have direct exact shared cross-language vectors, direct rollback coverage spans every §45.6 case, and §38.20 combines 14 shared semantic invalid-action classes with stable `DEFINITION_REJECTED`, 23 deterministic structural/semantic schema mutations with pinned codes or faults, and dedicated invalid fixtures; Linux ARM64 execution remains unavailable |
| §39 generated tests | all listed generators plus determinism and continuation properties | complete for the bounded corpus: Python and independent Rust execute the same reproducible 411 cases spanning random valid linear action graphs, transition guards, rational rates and save/restore points, raw same-tick input-order permutations, freeze-token timing/domain/accrual combinations, retained rollback corrections, within-limit parent-child structures, generated Core arithmetic, effect aggregation permutations, candidate permutations, bounded interaction-rule ordering, cross-stage transition-versus-start arbitration, complete interaction-pipeline routing, materialization, reduction, and commit, and integrated progression, interaction, and effect fault containment under every policy; repeat digest and continuation equivalence are direct assertions |
| §40 migration | v1/v2 importer warnings and version rejection vectors | complete: bounded explicit-version importer, all required warning classes, deterministic evidence hashes, valid review-only PCAM-24 output, and fail-closed compatibility vectors |
| §41 repository | required structure and documentation | complete: every normative file and directory lane exists, packaged implementations have explicit stable adapters, archive lanes are non-authoritative, and a machine-checked layout manifest pins the inventory |
| §42 tooling | all eleven commands with stable machine-readable result codes | complete: one machine-readable matrix drives every command through a successful result and a command-specific rejected result, then verifies missing-argument handling for every command and a file I/O failure. Every path returns a canonical JSON envelope, a declared stable result code, and exit status zero or two as pinned by the vector; argument parsing never escapes as human-only stderr or `SystemExit` |
| §43 licenses | CC BY 4.0 spec, Apache-2.0 or MIT code, vector permissions, trademark separation | complete: per-path license map, full CC BY 4.0, MIT, and CC0 1.0 texts, attribution, separate trademark policy, and explicit patent statement |
| §44 security | bounded untrusted-data validation and adversarial vectors | complete: machine-checked evidence covers every listed untrusted surface and bound, including pre-ingestion tick-input bytes, definition/snapshot bytes, runtime collections, nesting and parent cycles, candidates/effects/redirects, identifiers, integers, recursion, and extensions; hash authentication and extension sandboxing are explicitly disclaimed |
| §45 release gates | eight evidence reports with no unresolved requirement | partial: specification, schemas, rollback, bounded comparative experiment, and current-documentation claims gates are closed; independent-language execution evidence now includes a machine-checked source, dependency, bridge, subprocess, symlink, build-script, and generated-include audit; complete runtimes and ARM64 remain open |

## Completion rule

Search results, passing smoke tests, and plausible behavior are not enough. Each row requires the authoritative artifact and validation scope named above.
